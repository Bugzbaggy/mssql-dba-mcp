{
  "name": "appdb-sql-mcp",
  "version": "1.0.0",
  "description": "SQL Server DBA monitoring MCP server with read-only T-SQL and dbatools health-check access",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "esbuild src/index.ts --platform=node --target=node22 --bundle --packages=external --outfile=dist/index.js",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"tests/*.test.ts\"",
    "sync:docs": "node scripts/sync-schema-docs.mjs",
    "build:report": "esbuild src/report-cli.ts --platform=node --target=node20 --bundle --outfile=dist/report-cli.cjs",
    "package:report": "npm run build:report && node --experimental-sea-config sea-config.json && node -e \"require('fs').copyFileSync(process.execPath,'dist/appdb-sql-report.exe')\" && npx postject dist/appdb-sql-report.exe NODE_SEA_BLOB dist/sea-prep.blob --sentinel-fuse NODE_SEA_FUSE_fce680ab2cc467b6e072b8b5df1996b2"
  },
  "dependencies": {
    "@azure/identity": "^4.5.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.21.0",
    "jose": "^6.0.0",
    "mssql": "^11.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/mssql": "^9.1.5",
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.1",
    "postject": "^1.0.0-alpha.6",
    "tsx": "^4.19.0",
    "typescript": "^5.4.0"
  },
  "overrides": {
    "hono": "^4.12.25"
  }
}
